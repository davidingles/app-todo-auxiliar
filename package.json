{
  "name": "app-todo-auxiliar",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend run start",
    "check-dbs": "npm --prefix database run check",
    "generate-ico": "npm --prefix backend run generate-ico"
  }
}
